{
  "name": "bug-hunt-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Personal bug bounty hunting tracker — Chrome MV3 extension. No build step; package.json exists only so the pure-logic tests can run under Node.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "test:e2e": "node test/e2e.mjs",
    "test:all": "npm test && npm run test:e2e"
  },
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
